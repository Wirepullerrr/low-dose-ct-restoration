"""Learned restoration models.

Deliberately not re-exported from :mod:`ct_restoration.data` or anywhere else:
a model is a method under test, not part of the benchmark definition, and the
evaluation path must stay importable without one.
"""
